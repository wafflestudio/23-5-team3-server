import { Language } from 'web-tree-sitter';
/**
 * Set a custom base path for WASM files.
 * Used in bundled environments where WASM files are copied to a custom location.
 */
export declare function setWasmBasePath(basePath: string): void;
export declare function loadLanguage(langName: string): Promise<Language>;
//# sourceMappingURL=loadLanguage.d.ts.map