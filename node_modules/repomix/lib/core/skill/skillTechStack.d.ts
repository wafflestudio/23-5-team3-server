import type { ProcessedFile } from '../file/fileTypes.js';
interface DependencyInfo {
    name: string;
    version?: string;
    isDev?: boolean;
}
interface RuntimeVersion {
    runtime: string;
    version: string;
}
interface TechStackInfo {
    languages: string[];
    frameworks: string[];
    dependencies: DependencyInfo[];
    devDependencies: DependencyInfo[];
    packageManager?: string;
    runtimeVersions: RuntimeVersion[];
    configFiles: string[];
}
/**
 * Detects tech stack from processed files.
 * Only checks root-level dependency files.
 */
export declare const detectTechStack: (processedFiles: ProcessedFile[]) => TechStackInfo | null;
/**
 * Generates tech-stack.md content from detected tech stack.
 */
export declare const generateTechStackMd: (techStack: TechStackInfo) => string;
export {};
//# sourceMappingURL=skillTechStack.d.ts.map