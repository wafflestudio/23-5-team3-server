import fs from 'node:fs/promises';
import type { SkillOutputResult } from './packSkill.js';
/**
 * Writes skill output to the filesystem.
 * Creates the directory structure:
 *   <skillDir>/
 *   ├── SKILL.md
 *   └── references/
 *       ├── summary.md
 *       ├── project-structure.md
 *       ├── files.md
 *       └── tech-stack.md (if available)
 */
export declare const writeSkillOutput: (output: SkillOutputResult, skillDir: string, deps?: {
    mkdir: typeof fs.mkdir;
    writeFile: typeof fs.writeFile;
}) => Promise<string>;
//# sourceMappingURL=writeSkillOutput.d.ts.map